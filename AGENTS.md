# AGENTS.md

> 本文件是 opencode 的项目上下文，启动时自动读取。
> 在此描述项目意图、约定、命令与结构，让 agent 行为更贴合本项目。
> 可用 `opencode.json` 的 `instructions` 字段追加更多上下文文件。

## 项目简介

<!-- 一句话说明这个项目是做什么的、面向谁 -->
TODO：填写项目名称与目标。

- **名称**：
- **类型**：通用（语言/技术栈待定）
- **目标**：

## 目录结构

```
project/
├── AGENTS.md          # 本文件：项目上下文与约定（opencode 自动读取）
├── opencode.json      # opencode 配置
├── .opencode/         # opencode 扩展：agent / command / skill
│   ├── agent/
│   ├── command/
│   └── skill/
├── src/               # 源代码
├── tests/             # 测试
└── docs/              # 文档
```

## 开发命令

<!-- opencode agent 执行任务前会参考这里。确定技术栈后请补充实际命令 -->

- 安装依赖：
- 运行：
- 测试：
- 代码检查 / 类型检查（lint / typecheck）：
- 构建：

> 约定：完成任务后，如项目提供了 lint / typecheck 命令，必须先运行再交付。

## 代码约定

- 不主动添加注释，除非用户明确要求。
- 遵循已有代码风格：命名、缩进、导入顺序与周边文件保持一致。
- 新建文件前先查看同类现有文件，沿用其框架与库选择。
- 不暴露或提交密钥、令牌等敏感信息。

## Git 约定

- 未经用户明确要求，不要执行 commit / push / amend。
- 提交信息遵循仓库既有风格（简洁、祈使语气）。
- 仅暂存与本次改动相关的文件。

## 注意事项 / 项目特有规则

<!-- 例如：特定目录禁止改动、必须兼容某版本、性能约束等 -->
TODO：补充本项目独有的约束。
